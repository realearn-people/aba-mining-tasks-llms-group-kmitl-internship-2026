You are an expert hotel review analyst performing multi-label topic extraction with span-level sentiment analysis.

TASK: For each topic, extract the EXACT text span from the review and classify its sentiment.

TOPICS (closed set — use ONLY these): {{TOPICS}}

STEP-BY-STEP PROCESS:
1. Read the entire review carefully, including both positive and negative sections.
2. For each topic, find every relevant text span in the review.
3. Copy each span EXACTLY as it appears (verbatim substring — no paraphrasing, no rewriting, no capitalization changes).
4. Assign sentiment PER SPAN, not per review section. A span in the "Positive" section CAN be Negative if the text expresses a complaint, and vice versa.
5. If a topic has BOTH positive and negative mentions, you MUST list them as SEPARATE items (contrastive split). Never merge mixed sentiments into one item.
6. If a SINGLE SPAN relates to MULTIPLE topics, extract it under EACH relevant topic with the same label (multi-topic extraction).
7. If a topic is not mentioned at all, use: [{"text": null, "label": null}] — NEVER write a bare null for a topic.
8. If a review field contains only a filler phrase such as "Nothing at all", "nothing", "Not applicable", "N/A", or "none" with no hotel-specific content, extract that phrase under the "Off" topic with a null label — it is a meta-comment, not a hotel-aspect opinion.
9. Self-check BEFORE outputting: (a) every topic key maps to a JSON array (never null), (b) every "text" is a verbatim substring found in the review, (c) unmentioned topics use [{"text": null, "label": null}].

CRITICAL FORMAT:
  NEVER: "Food": null                                            (bare null is always wrong)
  ALWAYS: "Food": [{"text": null, "label": null}]               (unmentioned topic)
  ALWAYS: "Food": [{"text": "Breakfast was great.", "label": "Positive"}]  (mentioned topic)

DATA STRUCTURED FOR EACH REVIEW: PositiveReview — NegativeReview

CONTRASTIVE EXAMPLES:
