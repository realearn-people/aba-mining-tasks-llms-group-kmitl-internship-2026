# Paths are relative to the ABA_Mining/ directory unless absolute.
# Use forward slashes (works on both macOS and Windows via Python pathlib).

input:
  # Input dataset for Task 1 (reviews to process)
  input_csv: ../Dataset/A-1. hotel in Larnaca-Cyprus - A-1. hotel in Larnaca-Cyprus.csv

  # Gold/evaluation dataset (human-annotated ground truth for evaluation)
  gold_csv: ../Dataset/Original ABA Dataset for Version 2 [Oct 23, 2025], Senior Project, MUICT - Sheet2_.csv

output:
  task1_dir: outputs/task1
  eval_dir: outputs/eval
